import BaseCommand from '../../BaseCommand';

export default class Create extends BaseCommand {
	static description = 'Create metadata cache for a file or directory structure';

	async run() {
		throw new Error('Not implemented');
	}
}
