import BaseCommand from '../../BaseCommand';

export default class Update extends BaseCommand {
	static description = 'Update existing metadata cache';

	async run() {
		throw new Error('Not implemented');
	}
}
